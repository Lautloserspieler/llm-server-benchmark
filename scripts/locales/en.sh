# English texts for the Linux/macOS scripts, read by scripts/lib/ui.sh.
# Format: [key]="text" - printf-style placeholders (%s). Keys must match de.sh (checked by tests).
[ui.yes_key]="y"
[ui.yes]="yes"
[ui.no]="no"
[ui.install_question]="%s is missing. Download and install it automatically now?"
[ui.auto_install]="Installing %s (LLMBENCH_AUTO_INSTALL=1)."
[ui.choice_prompt]="Choice [1-%s, Enter = %s]:"
[setup.title]="LLM Server Benchmark – Setup (Linux/macOS)"
[setup.mode]="Execution mode: %s"
[setup.invalid_mode]="LLMBENCH_EXECUTION_MODE must be auto, docker or native."
[setup.done_docker]="Setup complete (Docker + CUDA)."
[setup.done_native]="Setup complete (native)."
[setup.done_hint]="To run the benchmark, just start START_BENCHMARK.sh."
[setup.docker_forced_failed]="Docker mode was forced but could not be set up."
[setup.docker_fallback]="Docker/CUDA could not be set up completely – falling back to native mode."
[setup.docker_unsupported]="Docker mode is not supported automatically on this system."
[setup.python_missing]="Python 3.10+ (with venv) was not found."
[setup.python_manual]="Please install Python 3.10 or newer (incl. venv) and run the script again."
[setup.section_packages]="Python environment and packages"
[setup.venv_create]="Creating virtual environment (.venv) ..."
[setup.pip_install]="Installing/updating llmbench and dependencies ..."
[setup.packages_ready]="Program and dependencies are ready."
[setup.llama_check]="Checking/installing a matching llama.cpp build ..."
[setup.llama_hint]="On Linux/NVIDIA, CUDA is preferred automatically once the driver and CUDA Toolkit (nvcc) are present; Vulkan and CPU remain fallbacks."
[setup.section_models]="Choose benchmark models"
[setup.section_config]="Configuration"
[start.title]="LLM Server Benchmark"
[start.docker_detected]="Docker + CUDA runtime detected."
[start.docker_setup_now]="Docker mode is forced but not ready yet. Setting it up now ..."
[start.docker_repair]="Docker/NVIDIA detected but the runtime is not ready yet – trying an automatic repair ..."
[start.docker_repair_failed]="Docker repair failed – using native mode."
[start.docker_not_ready]="Docker mode is not ready on this system."
[start.llama_failed]="Automatic llama.cpp installation/compilation failed."
[start.llama_failed_hint]="git, build-essential, cmake and pkg-config are required (plus the CUDA Toolkit with nvcc for CUDA). Alternatively place llama-bench and llama-server manually in tools/llama.cpp/."
[start.suite_title]="Standard suite"
[start.suite_download]="At least one selected model is missing or incomplete – downloading it from HuggingFace ..."
[start.doctor_title]="Pre-check"
[run.title]="Benchmark"
[run.duration_question]="How long should the test run?"
[run.duration_short]="short – quick check"
[run.duration_medium]="medium – default values"
[run.duration_long]="long – precise results"
[run.hardware_question]="What should be tested?"
[run.hardware_cpu]="CPU only"
[run.hardware_gpu]="GPU only"
[run.hardware_gpu_overload]="GPU only (RAM overload allowed)"
[run.hardware_both]="CPU and GPU (incl. sustained load test)"
[run.stress_question]="Run additional stress tests (TTFT/multi-tenant/OOM/quant)?"
[run.selection]="Duration: %s · Hardware: %s · Stress tests: %s"
[docker.sudo_missing]="Root privileges are required, but sudo is not installed."
[docker.only_debian]="Automatic Docker installation is only performed on Ubuntu/Debian."
[docker.no_codename]="The Linux distribution codename could not be determined."
[docker.declined]="%s is required for Docker mode, but the installation was declined."
[docker.install_engine]="Installing Docker Engine + Buildx + Compose from the official Docker repository ..."
[docker.install_compose]="Installing the Docker Compose plugin ..."
[docker.no_nvidia]="No NVIDIA GPU or NVIDIA driver detected on the host."
[docker.toolkit_manual]="The NVIDIA Container Toolkit must be installed manually on this distribution."
[docker.install_toolkit]="Installing the NVIDIA Container Toolkit from the official NVIDIA repository ..."
[docker.configure_runtime]="Configuring the NVIDIA container runtime for Docker ..."
[docker.daemon_unreachable]="The Docker daemon is not reachable."
[docker.needs_driver]="Docker GPU mode requires a working NVIDIA driver on the host."
[docker.gpu_check]="Checking the NVIDIA GPU in the Docker runtime ..."
[docker.image_pull]="Pulling prebuilt benchmark image: %s"
[docker.image_ok]="Image pulled – skipping the local CUDA build."
[docker.image_pull_failed]="The image could not be pulled."
[docker.image_local]="Using the already present local image %s."
[docker.image_build_fallback]="No local image available – building the CUDA image locally."
[docker.image_build_forced]="LLMBENCH_DOCKER_BUILD_LOCAL=1: forcing a local CUDA build."
[docker.runtime_ready]="Docker/CUDA benchmark runtime is ready."
